<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >
    <solid android:color="@null"/>
    <corners android:bottomRightRadius="@dimen/round_box"
        android:bottomLeftRadius="@dimen/round_box"
        android:topLeftRadius="@dimen/round_box"
        android:topRightRadius="@dimen/round_box"

        />
    <solid
        android:width="1dp"
        android:color="@color/btn_login_round_color" />
</shape>